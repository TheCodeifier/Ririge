{% extends base.html.twig %}

{% block body %}
    <nav class="navbar navbar-expand-lg bg-body-tertiary-light">
        <div class="container-fluid">
            <img class= "nbLogo" src="../img/Training_Factory_logo.png">
            <button class="nbToggler navbar-toggler position-relative" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse nbItems position-relative" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('Home') }}">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('Lesson') }}">Lessen</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('Account') }}">Account</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href= "{{ path('Contact') }}">Contact</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('About_Us') }}">Over ons</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <h1 class= "heading1 position-relative">Register</h1>




{% endblock %}